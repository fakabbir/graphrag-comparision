# cp to terraform.tfvars, or pass via TF_VAR_* env vars (preferred for secrets).
admin_cidrs = ["203.0.113.4/32"]   # your public IP /32 - scripts/tf.sh fills this in

# Secrets: DO NOT put these in a committed file.
#   export TF_VAR_neo4j_password='...'
#   export TF_VAR_deepseek_api_key='...'
